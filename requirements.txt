# Core dependencies for psws-drf-tid-tools
# All scripts assume Python 3.10 or newer.

# numpy is capped below 2.3 to match what scipy (below) actually
# supports -- confirmed directly: an unconstrained optional dependency
# (astropy) silently pulled numpy up to 2.5.1 during a real install,
# breaking scipy without any error until scipy was actually used.
# pip doesn't always see requirements.txt and requirements-optional.txt
# together when installed as two separate commands, so this needs to
# be pinned here explicitly, not left to what pip happens to resolve.
numpy>=1.24,<2.3
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
digital_rf>=2.6
requests>=2.28
beautifulsoup4>=4.11

# Interactive GUI tools (tid_spect_click.py, tid_quicklook.py)
PyQt5>=5.15
pyqtgraph>=0.13
Pillow>=9.0

# cwt-prophet extraction method (optional but recommended)
# Required for Pass 0 auto-run in tid_spect_click.py
# Install separately: pip install prophet
# prophet>=1.1
